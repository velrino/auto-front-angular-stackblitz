const ENUMS = {
  page_test: {
    page_test_input_cpf: {
      change(component) {
        console.log(component);
      }
    },
    page_test_input_pass: {
      change(component) {
        console.log(component);
      }
    },
    page_test_input_select: {
      click(component) {
        console.log(component);
      }
    },
    page_test_submit: {
      click(component) {
        console.log(component);
      }
    }
  }
};

export { ENUMS };
